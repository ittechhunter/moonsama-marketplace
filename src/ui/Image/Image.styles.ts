import makeStyles from '@material-ui/core/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  image: {
    width: '100%',
    height: 'auto',
  },
  imageNotShow: {
    display: 'none'
  }
}));
